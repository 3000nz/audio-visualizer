{
  "name": "audio-visualizer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wp": "vite build --config vite.wp.config.js",
    "make:plugin": "npm run build:wp && zip -r audio-visualizer-wp.zip audio-visualizer-wp/",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
